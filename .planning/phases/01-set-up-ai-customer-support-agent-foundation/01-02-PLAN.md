---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - shopify-widget/src/core/NetworkDetector.ts
  - shopify-widget/src/core/MessageQueue.ts
  - shopify-widget/src/ui/ChatWidget.ts
  - shopify-widget/tests/NetworkDetector.test.ts
  - shopify-widget/tests/MessageQueue.test.ts
  - shopify-widget/tests/ChatWidget.test.ts
autonomous: true
requirements:
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - "User receives a friendly fallback message when simulating a network or API timeout"
  artifacts:
    - path: "shopify-widget/src/core/NetworkDetector.ts"
      provides: "Network state monitoring"
    - path: "shopify-widget/src/core/MessageQueue.ts"
      provides: "Offline message caching"
    - path: "shopify-widget/src/ui/ChatWidget.ts"
      provides: "Main UI orchestration"
  key_links:
    - from: "shopify-widget/src/ui/ChatWidget.ts"
      to: "shopify-widget/src/core/NetworkDetector.ts"
      via: "status listener"
      pattern: "NetworkDetector"
    - from: "shopify-widget/src/ui/ChatWidget.ts"
      to: "shopify-widget/src/core/MessageQueue.ts"
      via: "message buffering"
      pattern: "MessageQueue"
---

<objective>
Implement the core vanilla TypeScript widget engine, including network detection, message queueing, and DOM orchestration.

Purpose: Ensures the chat widget is resilient to network failures and handles DOM updates without heavy frameworks.
Output: Core TS files and their corresponding unit tests.
</objective>

<execution_context>
@C:/Users/KIIT0001/Documents/antigravity skills/AI Customer Support Agent for Commerce/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/Documents/antigravity skills/AI Customer Support Agent for Commerce/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-set-up-ai-customer-support-agent-foundation/01-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client | App embed runs entirely in untrusted browser context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | MessageQueue | mitigate | Do not store sensitive auth tokens in localStorage queue |
| T-01-03 | Spoofing | ChatWidget | mitigate | Sanitize all incoming messages before injecting into DOM |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build NetworkDetector</name>
  <files>shopify-widget/src/core/NetworkDetector.ts, shopify-widget/tests/NetworkDetector.test.ts</files>
  <read_first>
    - .planning/phases/01-set-up-ai-customer-support-agent-foundation/01-RESEARCH.md
    - shopify-widget/src/core/NetworkDetector.ts
  </read_first>
  <behavior>
    - Test 1: Instantiates with current `navigator.onLine` status
    - Test 2: Fires callback with `false` when window 'offline' event occurs
    - Test 3: Fires callback with `true` when window 'online' event occurs
  </behavior>
  <action>
    Implement `NetworkDetector` class that registers `online` and `offline` event listeners on the `window` object. It must accept a callback function in its constructor and invoke it with the new boolean status whenever the network state changes.
  </action>
  <verify>
    <automated>cd shopify-widget && npm run test -- NetworkDetector</automated>
  </verify>
  <acceptance_criteria>
    - `NetworkDetector.ts` exports the detector class
    - Class correctly binds to `window` offline/online events
    - `NetworkDetector.test.ts` passes successfully
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build MessageQueue for offline resilience</name>
  <files>shopify-widget/src/core/MessageQueue.ts, shopify-widget/tests/MessageQueue.test.ts</files>
  <read_first>
    - shopify-widget/src/core/MessageQueue.ts
  </read_first>
  <behavior>
    - Test 1: Adds a message to the internal queue array
    - Test 2: Retrieves all pending messages
    - Test 3: Clears the queue when requested
  </behavior>
  <action>
    Implement `MessageQueue` class to temporarily buffer user messages if the API is unreachable. Add methods `enqueue(message)`, `getPending(): message[]`, and `clear()`. Messages should be typed with a basic interface containing `id`, `text`, and `timestamp`.
  </action>
  <verify>
    <automated>cd shopify-widget && npm run test -- MessageQueue</automated>
  </verify>
  <acceptance_criteria>
    - `MessageQueue.ts` implements enqueue, getPending, and clear methods
    - Type interface for messages is exported
    - `MessageQueue.test.ts` passes successfully
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Orchestrate UI with ChatWidget</name>
  <files>shopify-widget/src/ui/ChatWidget.ts, shopify-widget/tests/ChatWidget.test.ts</files>
  <read_first>
    - shopify-widget/src/core/NetworkDetector.ts
    - shopify-widget/src/core/MessageQueue.ts
    - shopify-widget/src/ui/ChatWidget.ts
  </read_first>
  <behavior>
    - Test 1: Instantiates NetworkDetector and MessageQueue
    - Test 2: Injects a friendly fallback message into DOM when simulating an API timeout or offline event
  </behavior>
  <action>
    Implement `ChatWidget` class that orchestrates the UI. It must instantiate `NetworkDetector` and `MessageQueue`. When `NetworkDetector` reports offline, or an API call fails, it must render a fallback message into a designated message container element (e.g. div with class `.ksp-messages`).
  </action>
  <verify>
    <automated>cd shopify-widget && npm run test -- ChatWidget</automated>
  </verify>
  <acceptance_criteria>
    - `ChatWidget` connects `NetworkDetector` status changes to UI updates
    - Renders fallback message on offline state
    - `ChatWidget.test.ts` passes successfully
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm run test:coverage` passes successfully
- [ ] Network failures gracefully notify the user via UI
</verification>

<success_criteria>
- Core classes are fully tested and integrate correctly
- Fallback messages appear correctly on systemic failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-set-up-ai-customer-support-agent-foundation/01-02-SUMMARY.md`
</output>
