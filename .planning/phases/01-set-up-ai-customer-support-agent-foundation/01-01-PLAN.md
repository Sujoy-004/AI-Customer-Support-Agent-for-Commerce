---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - shopify-widget/package.json
  - shopify-widget/vitest.config.ts
  - shopify-widget/src/ui/styles.css
  - shopify-widget/demo/index.html
autonomous: true
requirements:
  - SAFE-03
must_haves:
  truths:
    - "User views chat interface styled exactly to DESIGN.md (Berkeley Mono, flat cream canvas, hairline borders)"
  artifacts:
    - path: "shopify-widget/src/ui/styles.css"
      provides: "CSS design system and variables"
    - path: "shopify-widget/demo/index.html"
      provides: "Demo page for visual verification"
  key_links:
    - from: "shopify-widget/demo/index.html"
      to: "shopify-widget/src/ui/styles.css"
      via: "stylesheet link"
      pattern: "link.*href=.*styles.css"
---

<objective>
Scaffold the Shopify App Embed widget directory, configure testing infrastructure, and establish the CSS design system for the chat interface.

Purpose: Provides the foundation for UI rendering and testing before adding complex logic.
Output: Configured project structure, CSS design system, and a static demo page.
</objective>

<execution_context>
@C:/Users/KIIT0001/Documents/antigravity skills/AI Customer Support Agent for Commerce/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/Documents/antigravity skills/AI Customer Support Agent for Commerce/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-set-up-ai-customer-support-agent-foundation/01-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client | App embed runs entirely in untrusted browser context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | CSS files | accept | Styles are aesthetic; merchant has DOM control anyway |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold shopify-widget project and testing</name>
  <files>shopify-widget/package.json, shopify-widget/vitest.config.ts</files>
  <read_first>
    - .planning/phases/01-set-up-ai-customer-support-agent-foundation/01-RESEARCH.md
    - shopify-widget/package.json (if exists)
  </read_first>
  <action>
    Initialize a `package.json` in the `shopify-widget/` directory. Add `vitest` and `typescript` as dev dependencies. Add standard scripts: `test` pointing to `vitest run`, and `test:coverage` pointing to `vitest run --coverage`. Create a basic `vitest.config.ts` configured for browser environment testing using `jsdom`.
  </action>
  <verify>
    <automated>cd shopify-widget && npm install && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `shopify-widget/package.json` contains `vitest` dependency and test scripts
    - `vitest.config.ts` includes `environment: 'jsdom'` setting
    - `npm install` runs successfully in `shopify-widget/`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Establish CSS design system</name>
  <files>shopify-widget/src/ui/styles.css</files>
  <read_first>
    - .planning/ROADMAP.md
    - shopify-widget/src/ui/styles.css
  </read_first>
  <action>
    Create `styles.css` with a CSS variables section attached to `.ksp-chat-widget`. Define variables for a flat cream canvas background (`#FDFBF7`), hairline borders (`1px solid #1A1A1A`), and typography using 'Berkeley Mono' or fallback to standard 'monospace'. Scope all CSS rules strictly under the `.ksp-chat-widget` class to prevent overriding merchant themes.
  </action>
  <verify>
    <automated>grep -c "\.ksp-chat-widget" shopify-widget/src/ui/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - File contains `.ksp-chat-widget` prefix for all rules to prevent CSS specificity wars
    - File contains CSS variable for `Berkeley Mono`
    - File contains CSS variable for flat cream canvas background
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create static UI demo page</name>
  <files>shopify-widget/demo/index.html</files>
  <read_first>
    - shopify-widget/src/ui/styles.css
    - shopify-widget/demo/index.html
  </read_first>
  <action>
    Create a static `index.html` file that imports the `../src/ui/styles.css` stylesheet. Scaffold a static HTML representation of the chat widget utilizing the `.ksp-chat-widget` class, including a header, a message list area containing at least one mock user message and one mock agent message, and an input area.
  </action>
  <verify>
    <automated>grep -c "styles.css" shopify-widget/demo/index.html</automated>
  </verify>
  <acceptance_criteria>
    - File links correctly to `../src/ui/styles.css`
    - HTML contains the `.ksp-chat-widget` wrapping container
    - HTML includes mock elements for messages and input
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `shopify-widget/` directory contains `package.json` and `vitest.config.ts`
- [ ] CSS is scoped to `.ksp-chat-widget`
</verification>

<success_criteria>
- Project structure initialized and tests can execute
- CSS design system matches the aesthetic requirements
</success_criteria>

<output>
After completion, create `.planning/phases/01-set-up-ai-customer-support-agent-foundation/01-01-SUMMARY.md`
</output>
